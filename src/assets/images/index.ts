import { ImageSource } from './type';
export { RemoteImage } from './type';
export type { ImageSource } from './type';

export const imageLogo: ImageSource = {
  imageSource: require('./source/logo.png'),
};
